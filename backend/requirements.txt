fastapi==0.136.3
uvicorn[standard]==0.48.0
faker
pydantic==2.13.4
python-multipart==0.0.29
httpx==0.28.1
anthropic==0.105.2
python-dotenv==1.2.2
pandas
openpyxl==3.1.5
pymupdf==1.27.2.3

# ── Postgres-backed auth (organisations/users/roles/sessions/audit_logs) ───────────
sqlalchemy==2.0.*
psycopg[binary]==3.2.*
alembic==1.13.*
argon2-cffi==23.1.*
boto3==1.34.*

# ── lease_abstraction_xts engine (app/services/lease_abstraction_xts) ──────────────
# New multi-model extraction pipeline (Gemini + OpenAI). openpyxl / pymupdf / python-dotenv
# above already cover its other Python deps.
google-genai==1.69.0
openai==2.23.0
pdf2image==1.17.0
pytesseract==0.3.13
Pillow==12.1.1
# pdf2image needs the Poppler system binary (scanned PDFs only):
#   macOS:  brew install poppler        Debian: apt-get install -y poppler-utils
# Set POPPLER_PATH in .env only if Poppler is not on PATH.

# System dependency (NOT a pip package) — required for scanned-PDF highlight OCR.
# Install the Tesseract engine + Indian language data on the host / Docker image:
#   macOS:   brew install tesseract tesseract-lang
#   Debian:  apt-get install -y tesseract-ocr tesseract-ocr-all
# Invoked via the `tesseract` CLI (no Python binding needed).
# All highlight geometry/OCR code uses only the Python standard library + pymupdf above.
